# Notebooks

The production analysis is intentionally **not notebook-dependent**. Reusable logic lives under `src/causal_utility/`, experiments are driven by YAML configs, and aggregate outputs are generated by the CLI.

If exploratory notebooks are added later, they should import the package rather than duplicate cleaning, DGP, synthesis, or estimator logic.
